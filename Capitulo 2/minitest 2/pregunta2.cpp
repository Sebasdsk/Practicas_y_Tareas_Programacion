#include <iostream>

int multiply(int x, int y)
{
    int product{ x * y}; 
}

int main()
{
    std::cout << multiply (4) << '\n';
    return 0;
}


// Que la funcion multiply no tiene un valor de retorno/esta mal declarado y que se llama a la funcion multiply en main con un solo argumento
